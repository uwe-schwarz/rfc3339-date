{
  "name": "rfc3339-date",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run",
    "checks": "pnpm run lint && pnpm run typecheck && pnpm run test && pnpm run build",
    "cf-typegen": "wrangler types",
    "format": "oxfmt src tests package.json tsconfig.json wrangler.jsonc",
    "lint": "oxlint .",
    "typecheck": "tsgo --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "hono": "^4.12.3"
  },
  "devDependencies": {
    "@types/node": "^25.3.1",
    "@typescript/native-preview": "7.0.0-dev.20260226.1",
    "oxfmt": "^0.35.0",
    "oxlint": "^1.50.0",
    "vitest": "^4.0.18",
    "wrangler": "^4.68.1"
  }
}
