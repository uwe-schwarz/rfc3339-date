{
  "name": "rfc3339-date",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "gen:css": "node scripts/generate-tailwind-css.mjs",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "predeploy": "pnpm gen:css",
    "deploy": "wrangler deploy",
    "build": "pnpm gen:css && wrangler deploy --dry-run",
    "cf-typegen": "wrangler types",
    "format": "oxfmt src tests package.json tsconfig.json wrangler.toml",
    "lint": "oxlint .",
    "typecheck": "tsgo --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "hono": "^4.12.2"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.2.1",
    "@types/node": "^25.3.0",
    "@typescript/native-preview": "^7.0.0-dev.20260225.1",
    "oxfmt": "^0.35.0",
    "oxlint": "^1.50.0",
    "tailwindcss": "^4.2.1",
    "tw-animate-css": "^1.4.0",
    "vitest": "^4.0.18",
    "wrangler": "^4.68.1"
  }
}
