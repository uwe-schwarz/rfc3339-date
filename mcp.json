{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "mcpServers": {
    "rfc3339-date": {
      "type": "stdio",
      "command": "node",
      "args": ["./server.mjs"],
      "cwd": "${PLUGIN_ROOT}"
    }
  }
}
